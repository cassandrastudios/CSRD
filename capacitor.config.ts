import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cassandrastudios.apneatrainer',
  appName: 'Apnea Trainer',
  webDir: 'dist'
};

export default config;
