import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cassandrastudios.deepdivetrainer',
  appName: 'Deep Dive Trainer',
  webDir: 'dist'
};

export default config;
